#  llm-security-tester
CLI and web-based security testing tool that probes LLMs for prompt injection, jailbreaking, system prompt extraction, data exfiltration, and role confusion attacks — with a results dashboard and JSON report export.
